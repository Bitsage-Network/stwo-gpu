[package]
name = "stwo_cairo_air"
version = "0.1.0"
edition = "2024_07"

[lib]
casm = true

[features]
poseidon252_verifier = ["stwo_verifier_core/poseidon252_verifier"]
qm31_opcode = ["stwo_verifier_core/qm31_opcode"]
blake_outputs_packing = ["stwo_verifier_core/blake_outputs_packing"]
poseidon_outputs_packing = ["stwo_verifier_core/poseidon_outputs_packing"]
# Production fixed-program verifier profile. This removes optional Cairo AIR
# components that the conversation verifier executable must not use.
conversation_pruned = []


[tool.fmt]
sort-module-level-items = true

[tool]
cairo-lint.workspace = true

[dependencies]
bounded_int = { path = "../bounded_int" }
stwo_verifier_core = { path = "../verifier_core" }
stwo_constraint_framework = { path = "../constraint_framework" }
stwo_verifier_utils = { path = "../verifier_utils" }

[dev-dependencies]
cairo_test = "2.12.2"
