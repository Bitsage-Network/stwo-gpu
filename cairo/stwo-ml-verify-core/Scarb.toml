[package]
name = "stwo_ml_verify_core"
version = "0.1.0"
edition = "2024_07"
description = "Pure verification logic for ML inference proofs (sumcheck + layer chain)"

# Zero starknet dependencies — only uses core::poseidon

[dev-dependencies]
cairo_test = "2.12.2"

[cairo]
enable-gas = true
